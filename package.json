{
  "name": "aidbridge",
  "version": "1.0.0",
  "description": "AidBridge - Decentralized Aid Distribution Platform",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test jest --config=jest.config.js",
    "test:watch": "npm test -- --watch",
    "test:coverage": "npm test -- --coverage",
    "start": "node server/index.js",
    "dev": "nodemon server/index.js"
  },
  "dependencies": {
    "stellar-sdk": "^13.3.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "jest": "^29.0.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.2.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
